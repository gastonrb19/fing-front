
- [x] Test husky WF en frontend. (100%) (Fecha: 2026-08-16)
- [x] Crear WrapCardMovement.tsx. Copiar la estructura del form pero que no este disponible para editar. Que haya un estado que válide para la edición, este estado se manejara a traves de un boton que permitira con un onClick la edición del formulario (lo activará). La idea es que tenga una estructura visual semejante al FormTransaction.tsx; Además que cuando se active el form se active el botón para guardar (que hará el submit con los datos editados) y el botón cancelar, que permitirá volver a poner el estado en modo visualización. Quiero también que en cojunto con este modo, se visualice en algún sector (idealmente superior) el estado actual (edición | visualización). (Fecha: 2026-08-29)
- [x] Conectar al router una nueva ruta dinamica (dynamic segment) /movement/:id ; acá deberá estar incrustado el componente WrapCardMovement.tsx; Hay que generar la carpeta Movement y el archivo WrapCardMovemnt debe estar ahí dentro en conjunto con el archivo que lo contendrá (Movement.tsx). Movement.tsx es el que se debe llamar en el router de manera dinamica.  (Fecha: 2026-08-29)
- [x] Genera dentro del WrapCardMovement una nueva sección que contenga las cuotas. Quiero que tenga un tamaño especifico, pero que si excede dicho tamaño se pueda desplazar el usuario para mirar las diferentes cuotas. En la cuota debe ir el monto y además el usuario asociado al pago de dicha cuota.  (Fecha: 2026-08-29)
- [x] La logica de edición del WrapCardMovement y también del position se puede encapsular en un componente y llamarlo de vuelta, haz esto para que no quede como un spaguetti gigante. (Fecha: 2026-08-29)
- [x] Genera una nueva ruta dinamica (dynamic segment) /movement/:id/position/:id_position; Aquí dentro irá un desgloce de la cuota. Es lo mismo que en el movement. Que lo puedan editar, aparezca el boton cancelar, la visualización y que sea un form. Agregalo al router y también genera la carpeta para dicha ruta, además de generar el archivo que contenga dicha logica Position.tsx (Position debe llamarse la carpeta) (Fecha: 2026-08-29)
- [x] El CardMovement, cuando lo deslicen hacia la izquierda quiero que te lleve a la ruta de dicho movimiento para visualizarlo (la ruta dinamica). Quiero una pequeña animación para lo mencionado. Pero también quiero que se utilice el display card para que se despliegue un link dentro del card en la parte inferior en conjunto con la fecha. Esta funcionalidad esta anclada al estado, la otra no.  (Fecha: 2026-08-29)
- [x] Componente para volver atrás que se situe en la esquina superior izquierda con z-index por encima. (SharedComponent) (Fecha: 2026-08-29)
- [x] Componente de busqueda, al igual que el componente menu que tiene un despliegue con combinación de teclas, este se debe desplegar con la combinación "Ctrl + f". Este debe ser un input de texto y tiene que tener en cuenta lo siguiente. Se debe desplegar por encima de la app en el centro de la pantalla utilizando el 90% del ancho. Debe tener un placeholder que diga "@enter-command". Este generará busquedas directamente en la ruta /movements?text=<valorbuscado>. Pero si agregaran @<numero> se debe realizar una busqueda del query así: /movement/<numero>. Además para poder cerrar esto hay que presionar la tecla Esc o también pone un botón "X" arriba 10px. También agregalo al Menu y pasale dicho estado para desplegarlo también como boton y opción "Busqueda" (Fecha: 2026-08-29)
- [x] A la carpeta Transaction cambiarle el nombre a NewMovement y además, realizar el cambio de los nombres de los componentes y todos los que están dentro a el nombre prefijo NewMovement. También a nivel código y donde lo estén llamando. Además también en el router. (Fecha: 2026-08-29)
- [x] Implementar vista dedicada de Cuotas y Cobros Pendientes del Usuario (`GET /users/:userId/installmentuserpayments`). (100%) (Fecha: 2026-08-29)
- [x] Agregar movimiento del card hacia la izquierda para abrirlo con más detalles. (Fecha: 2026-08-29)